import { createStyles, makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() =>
  createStyles({
    buttonContainer: {},

    languageSwitcher: {
      padding: 0,
      color: '#FFFFFF',
      borderRadius: 0,
    },
  })
);
